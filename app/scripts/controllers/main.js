'use strict';

/**
 * @ngdoc function
 * @name where2workApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the where2workApp
 */
angular.module('where2workApp')
  .controller('MainCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
