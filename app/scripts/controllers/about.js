'use strict';

/**
 * @ngdoc function
 * @name where2workApp.controller:AboutCtrl
 * @description
 * # AboutCtrl
 * Controller of the where2workApp
 */
angular.module('where2workApp')
  .controller('AboutCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
